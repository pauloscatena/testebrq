using TesteBrq;

// Initializes the application and defines the classifier workload
var classifier = Application.Start();

// Prints the results in the standard output
Application.PrintOut(classifier);
